export default {

    // 控制只能输入数字
        inputChange(data) {
        
        // // value[value2] = ''
       
        // //防止其他字符的输入
        // form[key] = form[key].replace(/^(?:1[0-2]|0?[1-9]):[0-5]\d:[0-5]\d$//[^\d.]/g, "");
        // //清除“数字”和“.”以外的字符
        // // value = value.replace(/\.{2,}/g, ".");
        // //只保留第一个. 清除多余的
        // // value[value2] = value[value2].replace(".", "$#$").replace(/\./g, "").replace("$#$", ".");
        // // if (value[value2].indexOf(".") < 0 && value[value2] != "") {
        // //     //此处控制的是如果没有小数点，首位不能为类似于 01、02的金额
        // //     value[value2] = parseFloat(value[value2]);
        // // }
    },
}



